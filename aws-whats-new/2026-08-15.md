# AWS What's New: 2026-08-15

- **抓取时间:** 2026-08-15 09:07:47 UTC
- **过去 24h 公告数:** 1 ＋ **补录 2 条**（`ᴮ`，pubDate 早于 24h 窗口但从未进过任何 digest）
- **Source:** https://aws.amazon.com/about-aws/whats-new/recent/feed/

## 按类别详情

### Database (2 项)

| 时间 (UTC) | 公告 | 影响 |
|------|------|------|
| 08-14 15:00 | [Amazon RDS for Oracle now supports Oracle Application Express (APEX) version 26.1](https://aws.amazon.com/about-aws/whats-new/2026/08/amazon-rds-oracle-apex-26-1/) | Medium |
| 08-13 19:34 ᴮ | [Amazon Redshift adds rg.large and rg.12xlarge instance sizes in AWS GovCloud (US) Regions](https://aws.amazon.com/about-aws/whats-new/2026/08/amazon-redshift-adds-rg-large-12xlarge-aws-govcloud-regions/) | Low |

### 其他 (1 项)

| 时间 (UTC) | 公告 | 影响 |
|------|------|------|
| 08-14 07:00 ᴮ | [Amazon SES click tracking now supports custom URL paths for mobile app deep linking](https://aws.amazon.com/about-aws/whats-new/2026/08/amazon-ses-supports-customurl-deeplinking) | Medium |

## 小结与趋势

内容极少（周六），**本份真正的产出是两条流程上的东西**：一条昨天新加的机制第一次在实战中救回条目，一条分类器缺陷被定位并修掉。

### ⭐⭐⭐ 1. `BACKFILL_HOURS=96` 第一次实战命中，而且是「若无此机制则必然永久丢失」的形态

**SES 那条（pubDate 08-14 07:00）被昨天三次运行全部漏掉**，而它本该被覆盖 —— 昨天最后一跑是 11:48，其 24h 窗口是 08-13 11:48 → 08-14 11:48，**07:00 落在窗口正中**。核实方式：查 `2026-08-14.md` 里窗口内那 9 条，SES 不在其中 ⟹ **11:48 抓取时它还不在 feed 里，是之后才被补发进来的**。

- ⭐ **回填延迟界定**：它出现在 feed 里的时刻落在 08-14 11:48 → 08-15 09:04 之间，即 pubDate 之后 **4.8h–26.1h**。与此前观测到的「最大约 1 天」一致，**96h 的余量仍然充足**（但上界依旧未测到）。
- ⭐⭐⭐ **关键点是「若按旧口径必然丢」而不是「幸好补上了」**：今天的 24h 窗口下界是 08-14 09:04，已经滑过 07:00；此后每一天的窗口只会更晚。⟹ **在只有 24h 固定回看的旧逻辑下，这一条不会被任何一次运行看到。** 昨天写进 CLAUDE.md 的那句「回填 ＋ 固定 24h 回看＝永久静默丢失」今天拿到了一个**新鲜发生**的实例，而不再只是事后对账（07-31→08-13 那 25 条）推出来的。
- ⭐⭐ **与守卫（`MAX_RSS_AGE_MIN`）的性质差别今天也显出来了**：守卫抓的是「我这边读了旧输入」，是我的错误；回填是**上游的正常行为**，我这边没有任何一步出错可供断言检测 ⟹ 只能靠「把回看窗口放宽 + 按 link 去重」这种口径设计来兜，**而不是靠让失败变响**。

### ⭐⭐ 2. 「两源一致」在今天恰好演示了它为什么不能用来判断完整性

我按惯例核了第二数据源（dirs API）：**两源精确一致 —— 最新条目都是 08-14 15:00，08-15 都是零条。** 而今天的发现正是「08-14 有一条迟到条目」⟹ **两源今天同样都「不知道」它昨天缺**，因为它们读的是同一个上游、在同一时刻。

- ⭐⭐⭐ 这正好实证了 08-14 我推翻自己那句话时给出的判据：**多源核验只能证伪「我的读取有问题」，要证伪「上游不完整」必须在不同时刻重读同一个日期。** 今天两源一致的正确读法只有一条：**周六零条这个事实我读对了**；至于 **08-14 是否已完整，今天无法判断，只能明天复核**。
- ⚠️ 相应地，「08-14 全天 3 条」这个数字现在仍是下界。

### ⭐⭐ 3. 分类器：`support ` 是个通用英语动词，不是服务名

SES 被分到 **Management**，追下去是 Management 关键词表里的 `"support "` —— 而 `_kw_matches` 会 `strip()` 掉尾空格变成 `\bsupport\b`，于是描述里的 **"makes it easier to support mobile deep linking"** 直接命中。

- ⭐⭐ **与此前记过的两次漂移（WorkSpaces / Clean Rooms / Billing）不是同一种病**：那三次的根因是「该服务没有自己的关键词，于是漂到正文恰好提到的弱词上」；**这一次的根因是关键词本身就是个高频普通词** ⟹ 它不只影响没有关键词的服务，而是**给所有走到 Management 之前都没命中的条目提供了一个几乎必然命中的兜底**。
- ✅ **已修（窄化而非删除）**：`"support "` → `"aws support" / "support plan" / "support center"`，真正的 AWS Support 公告仍归 Management（已用三条测试标题验证）。
- ⭐ **按既有判据决定的**：改前先量影响面 —— **100 条 feed 里只翻转 2 条，且两条都是 Management → 其他**（SES 是邮件、Amazon Connect 那条是联络中心仪表盘，都不属 Management，兜底类才是诚实位置）。⭐ **其中 Connect 那条正是 08-14 我在评估「宽写法」时列为「明确错误」的那个附带翻转 —— 这次它被顺带修对了**，说明当时拒绝宽写法、只窄化 `cost management` 是对的，但漏掉了真正的病根。
- ⭐ **`health ` 同样可疑（"health checks" 满天飞）但本次 0 翻转，故不动** —— 没有证据就改属于凭猜调启发式。

### ⭐ 4. 一个我自己踩的小坑（与「不要用比被测系统更粗糙的工具」同型）

我用 `grep "redshift-adds-rg-large"` 查那条 GovCloud 公告是否重复，命中了 `2026-07-17.md`，一度以为 8 份回看窗口漏了去重。**实际是两条不同公告** —— 07-16 那条是 `2026/07/...instance-sizes`（实例规格上线），08-13 这条是 `2026/08/...aws-govcloud-regions`（进 GovCloud），**URL 不同，`_already_covered()` 行为正确**。⭐ 与 08-14 用裸 `in` "发现" 三个不存在的子串 bug 是同一形态：**用宽于被测逻辑的匹配去查被测逻辑，只会造出假问题。**

### 内容层面（一句话）

三条都是增量改进，无 High。⭐ 唯一值得留一眼的是 **RDS for Oracle 支持 APEX 26.1** 与 **Redshift GovCloud 加 `rg` 实例规格** —— 后者接上 08-13 那批 GovCloud 批次（Claude Opus 5 / Nova MME / Quick agentic AI 进 GovCloud），**GovCloud 补齐节奏本周持续**。

## Open Questions

1. ⭐⭐⭐ **08-14 明天复核会不会再涨？** 今天 3 条（其中 2 条是补录）已经说明 08-14 不完整过一次。**这是唯一能判断「08-14 全天到底几条」的办法**，也是本周第三次需要隔天复核（08-12 那次 6→9）。
2. ⭐⭐ **回填延迟上界仍未测到**。目前两个数据点：约 1 天（08-12 那三条）、4.8–26.1h（本次 SES）。`BACKFILL_HOURS=96` 是拍的余量；**若要收窄成有依据的值，需要记录每次补录条目的「pubDate → 首次观测」间隔**，攒够几十个点才能说上界。⭐ 成本很低（补录时已知两个时刻），值得加进脚本输出。
3. ⭐ **`health ` 要不要也窄化？** 本次 0 翻转故按纪律不动，但它与 `support ` 是同一类风险（普通词当关键词）。⭐ **判据应当是「等到它真的漂错一次再改」，而不是现在预防性改** —— 但值得在关键词表里把这类「普通词关键词」标注出来，以免下次又要从头追。
4. ⚠️ **周末零条这条规律又多一天支持，但它对「补录」不成立** —— 周六本身没有新公告，可周六的运行仍然产出 3 条。⟹ **「周末照跑」的理由现在比原来强：不是为了抓周末的公告，而是因为周末的运行是工作日迟到条目的接收窗口。**
