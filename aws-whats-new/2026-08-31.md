# AWS What's New: 2026-08-31

- **抓取时间:** 2026-08-31 09:11:08 UTC
- **过去 24h 公告数:** 0
- **Source:** https://aws.amazon.com/about-aws/whats-new/recent/feed/


过去 24h RSS 无新条目。

## 小结与趋势

### 「0 条」拆成两个可分别核验的来源

> ⭐ **本日两次运行（02:14 与 09:06）**，下文按抓取时刻标注；两次的 (a)/(b) 结论一致。

**(a) 上游确实没发 —— 强，且不需要任何解释性假设。** 独立解析 feed：**最新条目是 08-28 22:00 UTC（周五）的 `Amazon EC2 C8gn instances are now available in AWS Europe (Paris) region`，距 02:14 那次 52.2 小时、距 09:06 这次 59.1 小时。** 不是「差一点」而是差两天半。

⭐ **按日分布同时给了周末规律的第 N 次确认**（feed 里 100 条覆盖 08-18 → 08-28）：

| 日期 | 星期 | 条数 |
|---|---|---:|
| 08-24 | Mon | 11 |
| 08-25 | Tue | 16 |
| 08-26 | Wed | 6 |
| 08-27 | Thu | 13 |
| 08-28 | Fri | 8 |
| **08-29 / 08-30** | **Sat / Sun** | ⭐ **完全缺席** |

⚠️ 而 08-22 / 08-23（上一个周末）在这 100 条里同样完全缺席 ⟹ **本份一次给出两个周末的零产出确认。**

**(b) 窗口滑动 —— ⚠️ 本次不可检验，而原因正是「今天是周一」。** 脚本自报 **carried = 0**；而两个 24h 下界之间的区间是 **08-29 09:04（周六）→ 08-30 02:15（周日）**，feed 里该区间 **0 条**。

⟹ ⭐⭐⭐ **这正是我 08-30 立的那条判据所预测的形态**：**(a) 看本次窗口、(b) 看上一个窗口（比今天早一整天）**，故按星期排开时 **周一跑 (b) 才不可检验**（区间 ＝ 周六 → 周日，整段周末）。⟹ **今天的「0 条滑出」只能读作「本次无法检验」，不能读作「窗口设计没问题」。** ⭐ 而 (a) 强到 52.2 小时，所以本份的零依然是可靠结论 —— 这也印证那条「两个来源里有一个强结论就够下判断」。

### ⚠️ 两处我自己的口径错误（都被同一个习惯抓到）

**① 差点记下一个假的 `feed_span_h` 新低。** 我手算「最新 → 最老」得 **242.8h**，而脚本自报 **295.0h** —— ⭐ **差值恰好 52.2h ＝ 最新条目的年龄** ⟹ **脚本量的是「现在 → 最老」，我量的是「最新 → 最老」，两个不同口径。** ⟹ ⭐⭐ **按脚本口径 295.0 不是新低（最小值仍是 08-28 的 245.1）**；若我照自己那个数记，就会凭空造出一个「新低」并据此担心它跌破 168。⭐ **抓到它的唯一原因是拿我的统计去对脚本自报的数字**（该习惯第五次承重）。

**② 我的事后核验报出「超期区间 5 条未覆盖」，而脚本自报 0，脚本是对的。** 逐条回**全部 55 份** digest 核实（536 条规范化链接）后：**超期 46 条、未覆盖 0 条**，与脚本一致。
- ⭐⭐⭐ **根因是一个很细的时序差：`_already_covered()` 取最近 `BACKFILL_SCAN_FILES=12` 份并跳过 `out_path`。脚本在写盘前调用它，此时今天的文件还不存在，12 份 ＝ 08-19…08-30；而我在写盘后调用，今天的文件已存在并被计入再跳过，实际只剩 11 份覆盖 ⟹ 少了最老的那一天，那 5 条正落在里面。**
- ⟹ ⭐⭐ **可推广的一条：`_already_covered()` 在写盘前后给出不同答案，故它不能直接用于事后审计** —— 事后审计必须自己对全部 digest 建集合。⭐ 这与我记过三次的「复用辅助函数会继承它对『何时被调用』的假设」是同一族，**今天是它的第四次**（另一次是 `_prior_run_times()` 在写盘后把本次运行自己也算成了「上次运行」，输出「上次 = 本次」自相矛盾）。
- 🚨🚨🚨 **而我在 02:14 写的「这对脚本自身无害（它在写盘前调用）」这半句是错的，09:06 这次运行当场证伪了它 —— 见下一节。** ⭐⭐ **错在哪很具体：我只考虑了「一天跑一次」这个情形，而「写盘前」只对当天第一次运行成立；第二次运行时今天的文件已在磁盘上，脚本自己就落进了我刚描述的那个坑。** ⟹ ⭐ **把「无害」这个结论从一个我刚刚发现的时序缺陷里推出来，本该多问一句「什么情况下前提不成立」。**

### 🚨🚨🚨 09:06 那次：超期检查第一次报警，而它是假阳性 —— 根因就是上面那半句我说错的话

**09:04 那次运行的 stderr 打出了硬告警：「5 条超期公告在 feed 里但不在任何 digest 里 —— 正在被永久丢失」，并列出 5 条链接。** 这是 `_log_window_health()`（08-27 加的）自建立以来第一次非零。

**⭐ 而我没有信它，因为它与同一天 7 小时前那次的读数直接矛盾：02:14 那次是「超期 46 条 / 未覆盖 0」，7 小时后变成「47 条 / 未覆盖 5」，而这 7 小时里上游一条新公告都没发。逐条独立核验后确认全部 5 条都是假阳性：**

| 条目 | pubDate | 年龄 | feed 内出现次数 | URL 变体数 | 实际在哪份 digest |
|---|---|---:|---:|---:|---|
| `bedrock-agentcore-payments-ga` | 08-18 19:15 | 301.8h | 1 | 1 | ✅ `2026-08-19.md` |
| `amazon-corretto-august-2026-security-updates` | 08-18 21:48 | 299.3h | 1 | 1 | ✅ `2026-08-19.md` |
| `aws-iam-european-sovereign-cloud` | 08-18 22:30 | 298.6h | 1 | 1 | ✅ `2026-08-19.md` |
| `amazon-bedrock-openai-india-v1` | 08-18 23:30 | 297.6h | 1 | 1 | ✅ `2026-08-19.md` |
| `amazon-bedrock-grok-4-6` | 08-19 05:18 | 291.8h | 1 | 1 | ✅ `2026-08-19.md` |

⭐ **核验方式按 08-20 那条教训做（「答案对、机制错」只能靠回原始数据逐条核实才发现）**：自建**全部 56 份 digest / 741 条原始链接 / 537 条规范化链接**的集合、不复用 `_already_covered()`，并逐条确认每条在 feed 里**只出现一次、只有一种 URL 形式**（排除 `/about-aws/` 变体假点，那正是 08-20 造出三个假点的机制）⟹ **我的审计给出超期 47 条、未覆盖 0 条。**

**⭐⭐⭐ 根因是确定性的、不是巧合：`_already_covered()` 先 `files[:12]` 再跳过 `out_path`。** 实测两种切法拿到的文件名：

| 逻辑 | 份数 | 最老一份 |
|---|---:|---|
| 当前（先切 12 再跳过） | **11** | `2026-08-20.md` |
| 正确（先排除再切 12） | **12** | ⭐ **`2026-08-19.md`** |

⟹ **被挤掉的那一天恰好就是 `2026-08-19.md`，而那 5 条全在里面。**

**✅ 已修：抽出 `_recent_digests(out_path)`，先排除 `out_path` 再切片，`_already_covered()` 与 `_prior_titles()` 都改用它**（⭐ **后者本来没出症状，但有同一个洞——「给一类对象打补丁时要检查被排除的那一类」，这条判据这次救了第二个函数**）。修完 `over_window_uncovered` 5 → **0**，⭐ **而 digest 正文的影响面恰好只有抓取时间那一行**（`diff` 逐行确认）。

**⭐⭐⭐ 这次最该记的不是那个 off-by-one，而是「假阳性的硬告警比没有告警更糟」**：
- 这个告警的全部用途是回答「有没有条目正在被永久丢掉」，而它**每天第二次运行必报假警** ⟹ 若不修，我会在几次之后学会忽略它，而那时它就再也保护不了任何东西。
- ⭐⭐ **与 08-30 那条判据同族但更进一步**：那条说「改调度时要同时改检查它的东西，因为检查器错了的症状是安静」；**这次的症状不是安静而是吵，而吵的代价是把信噪比破坏掉。** ⟹ **两种症状都要防，且「吵」这一侧我此前没有记录过。**
- ⭐ **保住这个判断的唯一原因是同日两次运行给了我一个自相矛盾的对照（`0` vs `5`）** —— 与我反复记的「留一个结果显然错就会刺眼的对照量」是同一件事，只是这次那个对照量是**昨天/今早的同一个日志列**。⚠️ **若这个缺陷是在只跑一次的某天暴露的，我大概会当成真丢失去「加宽 `BACKFILL_HOURS`」，那是完全错误的动作。**

⚠️ **`window-health.tsv` 里 09:04 那行已标 `# INVALID` 并写明根因**（沿用 `backfill-delays.tsv` 的约定：**保留不删，因为它是当时下判断的依据**）；⭐ 已验证注释行不会被去重逻辑误读成时间戳。

### window-health 第 5–7 个读数

| run | feed_span_h | (96,168] 条数/未覆盖 | 超期条数/未覆盖 |
|---|---:|---|---|
| 08-27 09:09 | 300.0 | 21 / 0 | 46 / 0 |
| 08-28 09:04 | **245.1**（当前最小）| 7 / 0 | 48 / 0 |
| 08-29 09:04 | 253.8 | 16 / 0 | 46 / 0 |
| 08-30 09:04 | 277.8 | 27 / 0 | 46 / 0 |
| **08-31 02:14** | **295.0** | **33 / 0** | **46 / 0** |
| ~~08-31 09:04~~ | ~~301.8~~ | ~~33 / 0~~ | 🚨 ~~47 / **5**~~ ＝ **INVALID 假阳性** |
| **08-31 09:06**（修后） | **301.9** | **33 / 0** | **47 / 0** |

⟹ ⭐ **`(96,168]` 区间连续第 5 次全覆盖 ⟹ 截断假设仍未被证实**；判据不变（看上界分布的最大值，当前 88.3h / 第二高 86.5h）。⭐ **而 span 继续上下摆（300.0 → 245.1 → 253.8 → 277.8 → 295.0 → 301.9），最小值仍是 245.1，距 168 余 134h** —— 与我记的机制自洽：**跨过周末时 feed 不消耗条数而窗口下界不动，故 span 必然变长**，而本日两个读数正好在周末之后、且 7 小时内又涨了 6.9h（没有条目滑出，纯粹是「现在」在往前走）。

⚠️ **一个口径提醒（本日第二次踩同一个坑的机会）**：**`feed_span_h` 是「现在 → 最老」，所以在上游安静时它会随时间单调增长而 feed 内容一个字没变** ⟹ ⭐ **span 变长不等于 feed 变深；只有在有新条目进来时它才反映深度变化。** 本日 02:14 → 09:06 涨的 6.9h 就纯粹是时钟推进。

### 🚨 落盘时我自己造了一次 git 事故（并发仓库）

**① `git add -A` 把 tech-blogs 那次运行（08:59 start）正在写的 `tech-blogs/2026-W36b.md` 卷进了我的 commit。** ⭐ 是 `git show --stat` 的文件清单让我看见的 ⟹ **commit 后看一眼 `--stat`，清单里只该有本任务的产物。**

**② 而我去修正它时用了 `git reset --soft HEAD~1` —— 那一刻 HEAD 已经不是我的 commit：tech-blogs 在这几十秒里提交了自己那一笔，于是我撤掉的是别人的 commit。** ⭐ 靠 `git reflog` 找回、`git diff <sha> --stat` 确认工作树与它一致后 `git reset --mixed <sha>` 复原，**两个 commit 都在、工作树干净、未丢任何内容。**

⟹ ⭐⭐⭐ **判据：在并发仓库里 `HEAD~1` 不是「我刚才那个 commit」的可靠写法；要撤销自己的提交必须用 `git log` 里确认过的 sha，不要用相对引用。** ⭐ 而事后我**没有**再改写历史去追求「文件归属干净」—— 内容全在，**而在另一个会话可能仍在操作同一仓库时改写共享历史，正是刚才造成事故的那个动作。**

⚠️ **一个躲不掉的部分：`CLAUDE.md` 与 `ops/run-log.tsv` 本来就是四个任务共享的写入目标**，同分钟提交时 diff 会互相穿插，这不是 `add` 路径能避免的。

### ⚠️ 分类器与 Top Highlights

**本日 0 条，故两项都不适用**；⭐ **按「判断过但不需要覆盖时也明确写一句」那条规矩记下来，好让「检查过」与「忘了检查」事后可区分。**
