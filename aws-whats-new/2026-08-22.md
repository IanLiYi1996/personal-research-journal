# AWS What's New: 2026-08-22

- **抓取时间:** 2026-08-22 09:15:10 UTC
- **过去 24h 公告数:** 5 ＋ **补录 3 条**（`ᴮ`，pubDate 早于 24h 窗口但从未进过任何 digest）
- **Source:** https://aws.amazon.com/about-aws/whats-new/recent/feed/

## Top Highlights

- [Amazon Bedrock announces reduced pricing for OpenAI GPT-5.6 Sol](https://aws.amazon.com/about-aws/whats-new/2026/08/bedrock-openai-gpt-56-sol-reduced-pricing/) — AI/ML

## 按类别详情

### AI/ML (1 项)

| 时间 (UTC) | 公告 | 影响 |
|------|------|------|
| 08-21 20:11 | [Amazon Bedrock announces reduced pricing for OpenAI GPT-5.6 Sol](https://aws.amazon.com/about-aws/whats-new/2026/08/bedrock-openai-gpt-56-sol-reduced-pricing/) | High |

### Compute (3 项)

| 时间 (UTC) | 公告 | 影响 |
|------|------|------|
| 08-21 18:00 | [AWS Deadline Cloud now tracks automatic download status in the Deadline Cloud Monitor](https://aws.amazon.com/about-aws/whats-new/2026/08/aws-deadline-cloud-auto-download-status-tracking/) | Medium |
| 08-21 17:00 | [Amazon EKS Capability for Argo CD now supports custom configuration](https://aws.amazon.com/about-aws/whats-new/2026/08/amazon-eks-argo-cd-configuration) | Medium |
| 08-20 20:00 ᴮ | [Amazon EC2 C8gd, M8gd and R8gd instances are now available in additional AWS Regions](https://aws.amazon.com/about-aws/whats-new/2026/08/amazon-ec2-c8gd-m8gd/) | Low |

### Database (1 项)

| 时间 (UTC) | 公告 | 影响 |
|------|------|------|
| 08-20 16:00 ᴮ | [Amazon Aurora DSQL now supports Amazon CloudWatch Database Insights](https://aws.amazon.com/about-aws/whats-new/2026/08/aurora-dsql-cloudwatch-database-insights/) | Medium |

### Analytics (1 项)

| 时间 (UTC) | 公告 | 影响 |
|------|------|------|
| 08-21 16:42 | [AWS Glue 6.0 delivers 30% price reduction and Iceberg v3 support](https://aws.amazon.com/about-aws/whats-new/2026/08/aws-glue-6-0-price-reduction-iceberg-v3) | Medium |

### 其他 (2 项)

| 时间 (UTC) | 公告 | 影响 |
|------|------|------|
| 08-21 20:00 | [Amazon Connect Customer now lets managers chat with their data](https://aws.amazon.com/about-aws/whats-new/2026/08/amazon-connect-customer-ai-data-analytics) | Medium |
| 08-21 07:00 ᴮ | [Amazon SES now supports open and click tracking override parameters](https://aws.amazon.com/about-aws/whats-new/2026/08/amazon-ses-adds-open-click-tracking-override/) | Medium |

## 小结与趋势

### 窗口形态：周六，且这是正常形态不是缺口

今天是**周六**。窗口内 5 条的 pubDate 全部落在 **08-21 16:42–20:11 UTC**（周五下午到晚间），**08-22 本身 0 条**。这与「周末严格为零」（46 个有公告的日子里无一个周六/周日）一致 —— 本次运行的价值在于**接收周五傍晚那批**，而不是抓周六的产出。

⭐ 这也是 08-15 那条推论的又一次印证：**周末的运行是工作日迟到条目的接收窗口**，本次 3 条补录里有 1 条（SES）的 pubDate 就是周五 07:00。

### `BACKFILL_HOURS` 96→168 的第二次独立复核：那个区间仍然是空的

08-21 把 96 加宽到 168，理由是**测量性质**的：上界 = `now − pubDate`，故比窗口更长的延迟不是「未被测量」而是**不可见**；当时 8 个事件里有 2 个挤在 96h 天花板下方 8–10 小时，是被截断分布该有的形状，于是加宽以**检验那个区间到底有没有东西**。

本次换一天重跑同一检验：**feed 里落在 (96, 168]h 的条目共 14 条，14 条全部已被此前 digest 覆盖，0 条未覆盖。**

⟹ 连同 08-21 的 5/5 全覆盖，**加宽后两次都什么都没捞回来**。而这正是对那个截断假设的直接检验（一条延迟 100h 的条目会在此显示为 UNCOVERED）⟹ ⭐⭐ **倾向解释是「96h 附近那两个点是两个事件的巧合」而不是「分布被截断」**。⚠️ 但只有两天、且 168h 窗口只活了两天，不足以下定论；判据不变：**看上界分布的最大值**。

- 当前 **15 条有效行 / 10 个粗事件**，最大上界仍是 **88.3h**（08-14 那条 Amazon Quick），第二高 86.5h ⟹ 对 168 的余量 **79.7h、比值 1.90**（对 96 时只有 1.09）。
- 今天新增 3 个点全在 **26.1–41.1h**，都在窗口中段 ⟹ **减少**了「该继续升」的理由。

⚠️⚠️ **一处文档滞后需要修**：CLAUDE.md 的 AWS 小节正文仍写着「✅ 已修：`BACKFILL_HOURS = 96`」以及「该不该升现在只压在**一个**点上」，而代码自 08-21 起是 **168**、且近天花板的点是**两个**。⭐ 这与 08-21 抓到的那处不一致**形态相同**（CLAUDE.md 的定性 vs 代码里的论证），只是这次错的又是文档 ⟹ **改了常量要回写正文，不能只写进当天 digest 与 commit message。**

### 分类器：修掉一个「同一产品分到两个类别」的缺陷（本次是当场修，不是只记）

**AWS Deadline Cloud 此前没有关键词**，于是 feed 里的两条各自漂移：
- 「tracks automatic download status」只靠 **weak 通道的 `compute `** 命中正文里的 "compute-intensive workloads" 才到 Compute —— **与 08-14 那条 Billing→Compute 完全同一机制（被正文的偶然措辞决定）**，只是这次目的地恰好可辩护；
- 「Now Supports **EBS** Persistent Volume Cost Tracking」命中 `ebs` → **落到 Storage**。

⟹ 一个产品两个类别，**正是 08-13 记过、08-19 原样复现的 Amazon Quick 缺陷**。已按既有流程先量影响面：**恰好 1 处翻转、方向即目标、0 处附带误伤**（`ebs`/`fsx` 等正常条目不动），故采用：`deadline cloud` → **Compute**（理由与被否掉的两个替代方案已写进代码注释）。

⭐⭐ **这条值得记的是流程而不是结果**：08-20 我给自己立过「**记录一个已知缺陷 ≠ 修掉它**，发现分类瑕疵应当场量影响面并决定改不改，不要只写进小结」—— 本次是该规则第一次在发现的同一分钟内被执行。

### `health ` 关键词：仍不动，但测量方式换了且更该用这个

CLAUDE.md 一直把 `health ` 记为「同型可疑（"health checks" 满天飞）但本次 0 翻转，故不动」。今天它在 8 条里命中 2 条（Deadline Cloud、EKS Argo CD），看起来像该动了 —— 但**两次都没赢**，因为 Compute 在 `CATEGORIES` 里排在 Management 之前。

于是我改问一个更对的问题并在全量 feed 上量了：**`health ` 命中 2/100 条，而它是「唯一命中」的条目有 0/100 条。**

⟹ ⭐⭐ **对任何「普通词关键词」，该测的不是「它命中多少」而是「它有没有当过决定性命中」** —— 只有成为唯一命中时它才决定归类。按此判据它目前**决定不了任何东西**，故维持不动（判据仍是「等它真漂错一次」），但这个量比「本批 0 翻转」有信息得多：它解释了**为什么**目前无害（靠类别顺序），也就点明了风险条件（一条只命中 `health ` 的公告会错进 Management）。

### 🚨 心跳：抓到两处漏跑，其中一处是「检测器自己看不见的那类」

`runlog.py check` 报 ANOMALY，两条都独立核对过产出、不只信心跳：

1. **HF 08-21 完全没跑**（无 `start` 无 `done`）。独立核对：`research-notes/` 里最新 HF digest 是 **08-20 05:37** ⟹ 心跳正确。按 08-20 建立的形态判读，**无行＝会话开始执行之前就失败了**（prompt 第一行就是 `runlog.py start`），不是跑到一半死掉。
2. ⭐⭐⭐ **W34 cross-digest 该在周五 08-21 跑而没跑，`check` 原本一个字都不会说。** 独立核对：`weekly/` 最新仍是 **08-14 的 W33**。

⟹ 而第 2 条暴露的是**检测器本身的盲点，比这次漏跑更值得记**：`cross-digest` 同时满足两个条件 —— **从未写过行**（被 per-day 循环的 `task not in first` 跳过）**且不是 daily**（被 08-20 那条升级规则的 `kind == "daily"` 排除）⟹ **它在任何情况下都不可能被报出来，永久落在中性的 `not yet under observation` 里。**

- ✅ **已修**：闸门从「是不是 daily」换成「**它期望的那一天有没有在日志跨度内真的过去**」（过去了缺席才是证据；**今天排除**，因为安排在今天更晚的运行不算迟到）。修后 `check` 正确报出「expected day has passed 1x inside the log's span (latest 2026-08-21)」。
- ⭐⭐ **这与 08-20 那次是同一个盲点的第二半**：那次修的是「daily 任务在写下第一次心跳前就死掉」，**而修法把 weekly 排除在外，于是同一个洞在 weekly 上原封不动留着**。⚠️⚠️ 更难受的是我当时把「周任务不被升级」当成**一条防过度矫正的回归测试来庆祝** —— 它实际上是在把 bug 钉死。⟹ ⭐⭐⭐ **判据：给一类对象打补丁时，必须检查被排除的那一类是不是也有同一个洞；一条「某类对象不受影响」的回归测试，可能是在固化缺陷而不是在防误报。**
- ⭐ 六条回归全过（周五已过去→报 / 跨度无周五→静默 / 单日→静默 / **期望日恰是今天→静默** / cross-digest 真跑过→静默 / 空日志不崩），并确认 weekly 的「只有 `start` 没 `done`」仍能报出、daily 升级仍是 3 条、`check` 仍永远 exit 0。⚠️ 测试中我自己两次把断言写在错的轴上（拿「整体是否 ANOMALY」当断言，而**其他**任务缺席也会让它 ANOMALY）⟹ **断言必须落在被改的那个轴上。**
- ⚠️ **今天（08-22）截至 09:12 只有 AWS 写过行**，HF(07:57)/reddit(08:13)/tech-blogs(08:22) 三个时刻都已过去且都无行 ⟹ **「AWS 活、其他死」不对称形态很可能是第 5 次**，而 AWS 仍是四个里时刻最晚的那个。⚠️ 但 `check` 按设计不对「今天」下断言（更晚的运行不算迟到），故这条**目前只是我的观察而非检测结果**，明天的 `check` 才会确认。

### 内容：一天之内两个层次各降一次价，而方向与 DeepSeek 相反

- ⭐⭐⭐ **Bedrock 下调 OpenAI GPT-5.6 Sol 价格**：**$4/M 输入、$20/M 输出，输入降 20%、输出降 33.3%**，促销价**至少持续到 2026-11-21**；原文明说这是**继 Terra 与 Luna 降价之后**的第三次。⭐ 数字内部自洽（$5→$4 = 20%、$30→$20 = 33.3%），⭐⭐ **而三次降价的顺序是从便宜档位往上走 ⟹ 降价在沿能力阶梯上行，Sol 是目前最高的一档**。
- ⭐⭐ **AWS Glue 6.0 降价 30% + Iceberg v3** ⟹ **同一 24h 窗口里两个不同层次各降一次（模型推理 ⊕ 数据处理）**。

⭐⭐⭐ **这直接支持我在 [[weekly/2026-W33]] 做的那处更正**：当时 DeepSeek 涨价 50–1000% 与 RTX 6000 PRO 涨到 $16,000 出现时，我把主线拆成「**同等能力的单位成本**仍降（技术效率+竞争）」vs「**具体厂商的具体价格 / 单位算力采购价**可涨（商业决策+产能约束）」。本次是前者的新证据，且与后者**同期并存** ⟹ 两个方向同时为真，给客户材料的表述必须是前者那句，否则一条涨价新闻就能打掉整段论证。

### 一条产品侧的「证据面」数据点（⚠️ 厂商文案，未核实）

**Amazon Connect Customer「让经理用自然语言问数据」**的描述值得单记：它承诺返回的不只是答案，而是「**the answer, the evidence behind it, and the fix**」，并称跨 150+ 指标给出**带置信度分数与预期影响的优先级清单**。

⟹ ⭐⭐ 这是我追的「**不能信自我报告 → 证据面**」那条线在 BI/联络中心产品里的一个形态：**把「支撑该结论的证据」与结论一起交付**。⚠️⚠️ **但这是厂商营销文案、我未验证任何一项**，尤其「置信度分数」按什么校准完全未说明 —— 而按我这两周反复记的判据，**一个不说明校准方式的置信度分数正是最该追问的东西**。

### 抓取层面两条

1. ⚠️ **`/tmp/aws-rss.xml` 落盘时是 24h 旧的**（Aug 21 09:04），已按 slash command 第 1 步重抓后才跑脚本。若直接跑，`MAX_RSS_AGE_MIN = 60` 会打印重抓命令并 `return 2` ⟹ 守卫覆盖的正是这个形态（**cron 侧 curl 失败但旧文件仍在**）。本次不构成事故，只是记下守卫的适用场景在真实状态下长什么样。
2. ⚠️⚠️ **我自己又犯了一次「用比被测对象更粗糙的工具去测它」**：开头用 `grep -c '<item>'` 报了「89 条」，**而 `grep -c` 数的是匹配的行数不是出现次数**，这份 feed 把多个 item 放在同一行 —— 实际是 **100 条**。⭐ 没有影响任何结论（89 那个数我没用在任何地方），**但这是该失效模式第五次左右复现，频率没有下降** ⟹ 正确写法是 `grep -o '<item>' | wc -l` 或直接解析 XML。

## Open Questions

1. ⭐⭐⭐ **168h 窗口该不该收回去？** 两次复核都是「(96,168] 区间 0 条未覆盖」。若再攒几天仍为空，那 96→168 的加宽就应被理解为**一次已完成的测量**（结论：那个区间是空的）而不是需要长期保留的余量 —— 而代价是每次多扫的对账量。⚠️ 但先别动：只有两天，且 88.3h 与 86.5h 两个点距 96 太近，收回去会立刻回到比值 1.09。
2. ⭐⭐ **`其他` 里那两个常客该不该有类别？** 今天 2/8 落在 `其他`，都是没有类别的高频服务（**Amazon Connect** 与 **SES**）。两者都反复出现，但按「不为 n=1/n=2 新建类别」的既有判据现在不该动。⭐ 该做的是**记录频次**：等某一个在若干份 digest 里累计够多，再决定是新建类别还是钉进现有类别。
3. ⭐ **GPT-5.6 Sol 那个促销价 11-21 之后会怎样？** 原文写的是 "at least through November 21, 2026" ⟹ 这是**促销**而非结构性降价。⭐ 值得到期时回看一次，因为「促销价到期后是否回涨」正好区分「单位成本真的降了」与「竞争期的临时定价」，而这两者对客户材料的含义完全不同。
4. ⚠️ **Deadline Cloud 归 Compute 是「不算错但可能选错了轴」**（与 08-19 的 Console-to-Code 同型）。AWS 自己把它归 Media Services，而这里没有该类别。若后续 media/creative 类公告变多，应重新评估是否值得新建一类而不是继续塞 Compute。
