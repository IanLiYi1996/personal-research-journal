# AWS What's New: 2026-08-24

- **抓取时间:** 2026-08-24 09:07:59 UTC
- **过去 24h 公告数:** 0
- **Source:** https://aws.amazon.com/about-aws/whats-new/recent/feed/


过去 24h RSS 无新条目。

## 小结与趋势

### ⭐⭐⭐ 「0 条」已按两个来源分别核实，且两者结论强度不同

按 CLAUDE.md 那条判据（2026-08-14 起照此做），「0 新增」必须拆成 (a) 上游确实没发 / (b) 窗口滑动进出相抵，因为事后无法区分。本次：

| 来源 | 做法 | 结果 | 强度 |
|---|---|---|---|
| **(a) 上游确实没发** | 独立解析 RSS，看最新条目距抓取时刻多久 | ⭐ **最新条目是 08-21 20:11 UTC，距抓取时刻 60.9 小时** | **强**：不是「差一点」而是差两天半 |
| **(b) 窗口滑动** | 上次运行 24h 下界 → 本次 24h 下界之间有几条 | **0 条滑出**（区间 08-22 09:04 → 08-23 09:04） | ⚠️ **弱，见下** |

⚠️⚠️ **(b) 今天几乎不携带信息，这一点必须说清楚**：两个下界之间的区间（08-22 09:04 → 08-23 09:04）**整段落在周末内**，而周末按已确立的规律严格为零 ⟹ 「0 条滑出」是被周末规律强制的，不是对窗口设计的独立检验。⭐⭐ **判据：报 (b) 时要同时报那个区间跨的是哪几天——若整段落在已知零产出的时段，(b)=0 应当读作「本次无法检验」而不是「窗口设计没问题」。** 这是我第一次遇到 (b) 退化的情形。

- ⭐ 而 (a) 这次强得罕见：**60.9h 的静默不需要任何解释性假设**，最新条目就在那儿、时间戳明确。两个来源里有一个强结论就够下判断。
- ⭐ **feed 深度 10.97 天**（08-10 21:00 → 08-21 20:11，100 条）⟹ 远大于 168h 补录窗口，本次不存在「feed 条数用完」那类问题（那是 tech-blogs 侧 arXiv/量子位/LessWrong 的病，AWS 没有）。

### ✅ 周末严格为零：08-22(Sat) 与 08-23(Sun) 双源确认 0 条

RSS 与 dirs API（第二数据源，独立可翻页）**逐日计数完全一致**：08-21 Fri **6** / 08-20 Thu **13** / 08-19 Wed **20** / 08-18 Tue / 08-17 Mon，而 **08-22、08-23、08-24 三天各 0 条**。

- ⭐⭐⭐ **而这里有一个正确使用双源的例子，与一个不能这样用的对照**：
  - ✅ **08-22 与 08-23 的「0 条」是可以下断言的**，因为今天这次读取本身就是**对昨天那份 digest 断言的隔天复核**（在不同时刻重读同一个日期）—— 这正是 CLAUDE.md 要求的方式。昨天 08-23 那份报 0 条，今天复核仍为 0 ⟹ 昨天那个断言成立。
  - ⚠️⚠️ **而 08-24 自己的「0 条」不能下断言**：双源一致只能证伪「我的读取有问题」，证伪不了「上游不完整」。**08-18 就是这个形态的实例**——那天 09:06 两源都说「08-18 尚无条目」，隔天复核却是 11 条、其中两条 pubDate（04:00 / 08:00）比那次观测还早 5 小时以上。⟹ **本份关于 08-24 的一切计数均为暂定，须待明天复核。**

### ⭐⭐ 周一形态第 10 个数据点（暂定）：09:04 报 0 条是正常的，不是漏抓

CLAUDE.md 的节律统计：周一当天首条公告的 UTC 时刻**均值 10.2h / 中位 10.0h ＝ 各工作日里最晚**，而**首条早于 09:04 的比例周一仅 4/9 ＝ 44%**（Tue 10/10 ＝ 100%）；⭐ **但周一当天产量并不低（均值 10.2 条，高于周五的 6.1 条）** ⟹ **周一 09:0x 报 0 条说的不是「AWS 周一发得少」而是「周一那批还没开始发」**，条目会落进周二那份 digest（周二 09:04 的 24h 窗口覆盖周一 09:04–24:00）。

- 今天（08-24 Mon）09:04 观测 0 条 ⟹ **暂定使周一「首条早于 09:04」的比例变为 4/10 ＝ 40%**。
- ⚠️⚠️ **暂定二字是承重的，理由就是上一节那条**：08-18 那次正是「周二 09:06 观测 0 条 → 隔天复核 11 条且两条早于观测时刻」，当时那个 0 条差点被我写成 `Tue 9/10`。⟹ **明天必须复核 08-24 的真实条数与首条时刻，再决定这个数据点是 4/10 还是 5/10。**
- ⭐ 与此一致：**周五末条 → 周一首条的间隔当前为 60.9h 且区间尚未闭合**。CLAUDE.md 记的已闭合间隔是**均值 65.0h、n=8、范围 55.8–81.0h**，且**全部 499 个相邻间隔里最长的 6 个都是周五→周一** ⟹ 60.9h 完全是众数形态，**报静默时长前先问它跨不跨周末**这条判据在本次直接适用：今天这个 60.9h 不是信号。

### ⭐⭐⭐ `BACKFILL_HOURS = 168` 的第四次检验：仍为空，而样本量已显著变强——但我决定不收回

08-21 把 `BACKFILL_HOURS` 从 96 加宽到 168 时，理由是**测量性质的**（上界 = `now − pubDate`，故比窗口更长的延迟不是「未被测量」而是**不可见**；当时 8 个事件里有 2 个挤在 96h 天花板下方 8–10 小时＝被截断分布该有的形状）。加宽同时做两件事：捞回条目 **＋ 检验那个区间到底有没有东西**。

四次检验结果与样本量：

| 日期 | (96,168] 条目数 | 未覆盖 | 覆盖的日历天 |
|---|---|---|---|
| 08-21 | 5 | 0 | — |
| 08-22 | 14 | 0 | — |
| 08-23 | 26 | 0 | 三个工作日 ＋ 一个周末日（08-16） |
| **08-24（本次）** | **45** | **0** | ⭐ **08-17 Mon → 08-20 Thu，四天全部是工作日** |

另本次整个 `(0,168]` 范围 **63 条、0 条未覆盖**。

- ⭐⭐⭐ **今天这次是四次里最强的一次，理由不是 45 这个数字而是它没有周末稀释**：08-23 那次的 26 条对应 08-16→08-19、其中 08-16 是周末（0 条），有效覆盖只有三个工作日；今天的 45 条对应 08-17 12:58 → 08-20 00:04，**四天连续工作日、且横跨本月产量最高的 08-19（20 条）**。一条延迟 100h 的条目会在此检验中显示为「未覆盖」，而四次都没有。
- ⟹ ⭐⭐ **「分布被截断」这个假说现在很弱了**，倾向解释仍是「96h 附近那两个点（88.3h 与 86.5h）是两个事件的巧合」。
- 🚨⭐⭐⭐ **CLAUDE.md 写的收回判据（「若再攒几天仍为空，96→168 应被理解成一次已完成的测量」）在「空不空」这一侧现在已经满足，但我决定不收回，理由是代价不对称**：
  - 保留 168 的代价 = **每次多扫的对账量**（本次多扫 45 条，`_already_covered()` 读 12 份 digest，秒级完成）。
  - 收回到 96 的代价 = **任何延迟 >96h 的事件将永久静默丢失，且按定义不可见**（它进不了窗口，所以我不会知道自己丢了）。而已观测最大上界 **88.3h**、第二高 **86.5h**，距 96 都在 10 小时内 ⟹ 收回即刻回到比值 1.09。
  - ⭐⭐⭐ **判据（对 CLAUDE.md 那条的细化）：「区间是空的」只证明了「到目前为止没有超期事件」，它不改变「超期事件不可见」这个结构性事实。当一侧代价是廉价的重复劳动、另一侧代价是不可见的永久丢失时，空的检验结果不足以构成收回理由。** ⟹ 该常量按「便宜的保险」保留，而这四次检验的价值在于**它证明了 96 当年那个 8–36 小时边际大概是够的**，不在于让我去掉边际。
- ⭐ 与此相关的一条纪律本次也适用：**这些点存在的唯一目的是判断安全边际够不够，所以任何用于该判断的测量都要先确认它的偏差方向不是「让边际看起来更安全」**（08-18 下界算法系统性低估、08-20 的 `/about-aws/` 假点各是一个方向的实例）。本次检验的偏差方向是中性的——「未覆盖数」由 `_canon_link()` 归一化后判定，两种 URL 形式已合并。

### ⭐ 本次未触发的守卫（记录以便下次判断有依据）

- `MAX_RSS_AGE_MIN = 60`：抓取文件 mtime 与运行时刻相差 **0.4 分钟**，远未触发。⭐ 该守卫最主要的价值仍是覆盖「curl 失败但旧文件仍在」这个 cron 侧情形（2026-08-14 的静默假零）。
- `_prior_titles()` 标题告警：本次 0 条窗口内条目，无可比对象，未输出。
- ⚠️ **口径说明（免得日后对账时困惑）：header 的抓取时间是 `09:07:59`，而本份正文里 (a)/(b) 两项计算用的是首次运行的 `09:04:17`** —— 因为我为验证「merge-write 会不会保住手写章节」而重跑了一次脚本（结论：**68 行 prose 全部保住、两个章节都在、`backfill-delays.tsv` 未被追加**）。⭐ 两个时刻相差 4 分钟，而 (b) 那个区间内 08-22/08-23 根本没有任何条目，故结论不受影响；⚠️ **但明天算 (b) 时会从 header 取到 `09:07:59` 而非 `09:04:17`，这就是「重跑一次脚本会改写它自己的运行历史」这件事的一个具体后果**（与 08-18 那个「同日第二跑重复记账」是同一族问题的轻量版：merge-write 只保留每天最后一次的 header）。
- ⭐⭐ **而本次我刻意没有复用 `_prior_run_times()` 去算 (b)**，改为从昨天 digest 的 header 独立解析上次运行时刻 —— 因为**本次已写盘**，该函数会把本次运行自己算成「上次」（这正是 08-23 踩到的坑：当时得到「0 条滑出」而正确答案是 5）。⭐ 并且我把两个时刻都打印出来 ＋ 加了 `assert prev < now`，好让「上次＝本次」这种自相矛盾立刻刺眼 —— ⚠️ **08-23 的教训是：若只打印那个 0，它看起来完全合理（周日、上游没发、0 条滑出），我会把它当成 (b) 的答案写进 digest；一个错误地通过了的核验比没做核验更糟。**

### 🚨⭐⭐⭐ 心跳 `check` 输出：ANOMALY（三个日更任务共 7 个缺失日），而 AWS 恰在 09:04 起跑第 6 天——**这次跨过了 cron 重建**

```
RUNLOG ANOMALY
  observed: aws since 08-18, cross-digest since 08-24, hf since 08-20, reddit since 08-21, tech-blogs since 08-21  [24 rows, window 7d]
  hf:         NO RUN on 08-21 / 08-22 / 08-23   （连续三天）
  reddit:     NO RUN on 08-22 / 08-23
  tech-blogs: NO RUN on 08-22 / 08-23
```

- ⭐⭐⭐ **本次最该记的新信息：AWS 的 `start` 时刻是 09:04，而 cron 重建发生在今天 02:34（commit `c0e5e61`）** ⟹ **今天这次是重建后新建的那个 aws job（`a68ef715`）在它第一个排定时刻成功触发** —— 此前「AWS 恰在 09:04 起跑」已连续 5 天（08-19~08-23），⭐ **现在是 6 天，且这条链跨过了一次删除重建 ⟹ 「只有 AWS 活」不是某个特定 job 实例的运气，重建出来的新 job 同样活。**
  - ⭐⭐ 而对照仍然成立：**其余五个任务在重建前后都没有被 cron 触发过。** 唯一例外看起来是 cross-digest 今天写了行，⚠️ **但它的时刻是 02:20 而它的 cron 是周一 09:41 ⟹ 那是手敲补写 W34，不是 cron 触发。** ⟹ **cross-digest 至今 0 次 cron 触发这个事实没有变。**
  - ⭐ 顺带：**`observed: cross-digest since 08-24` 是 08-22 那个 weekly 闸门修复第一次真正生效的样子** —— 它此前因「从未写过行 ＋ 不是 daily」永久不可见，现在写了第一行、进入观测；而**今天正是它的期望日（周一），`check` 正确地没把它报成迟到**（09:41 还没到，「今天要排除」那条规则在起作用）。⟹ **09:41 是新 cross-digest job 的第一个检验点。**
- ⚠️ 三个日更任务的 7 个缺失日**全部早于今天 02:34 的重建**（hf 08-21/22/23、reddit 与 tech-blogs 08-22/23）⟹ 本次 ANOMALY 报的是重建之前的旧账，**不是对新 job 的判决**；它们各自的第一个检验点分别是今天 07:57/17:41（hf）、08:13（reddit）、08:22（tech-blogs）—— ⚠️⚠️ **而这三个时刻今天都已经过去，日志里却没有它们的行 ⟹ 新建的那三个 job 在今天的第一个排定时刻同样没有跑起来。** ⭐ 这把「重建修不了跑不起来」从上周的推断变成了本次的直接观测：**同一次重建产出的六个 job，AWS 那个在 09:04 准点跑了，另外三个在更早的排定时刻没跑。**
  - ⭐⭐ **而这恰好再一次否掉「排得晚更容易跑起来」**（该假说 08-23 已被 hf 17:41 否过一次）：今天跑起来的是 09:04，而没跑起来的三个排在 07:57 / 08:13 / 08:22 —— **都比 09:04 更早**。⟹ 方向看似支持「越晚越好」，⚠️ **但 hf 的 17:41 比 09:04 更晚且从未跑起来，故整体仍不成立；我给不出机制。**
  - ⚠️⚠️ **硬边界照旧**：「触发了但会话在第一条命令前就死」与「根本没触发」在产出上完全一样（都是无行）⟹ 只能断言「**只有 09:04 那个 job 产生过心跳行**」，不能断言另外几个「没被触发」。

## Open Questions

1. ⚠️⚠️ **最紧迫且不是内容问题：cron 约 2026-08-31 到期**（08-24 刚重建六个任务，`durable: true`）。而 08-18~08-23 那一周里**六个任务只有 AWS（09:04）真的被 cron 触发过**（连续 5 天恰在 09:04 起跑），其余五个 0 次 ⟹ **重建修不了「跑不起来」，但到期后若不重建，连 AWS 也会停——那是唯一还在工作的那一个。**
2. **08-24 的真实条数与首条时刻**（明天复核）。这决定：①周一「首条早于 09:04」是 4/10 还是 5/10 ②周五→周一间隔的第 9 个数据点落在哪（当前下界 60.9h，已闭合区间为 55.8–81.0h）。
3. `BACKFILL_HOURS` 的下一步：本次已决定不收回（代价不对称）。⭐ **但值得改的是别的东西——目前「区间是空的」这个结论完全依赖我每天手工跑那段检验代码。若把它做成脚本里的一行自动检查（未覆盖数 > 0 时打 stderr 告警），这个测量就会在我不再每天想起它之后继续有效。** 列为待办而非本次改动。
4. ⭐ 延迟日志 `backfill-delays.tsv` 当前有效点仅 **6 个事件**（9 个原始点中 3 个是 `/about-aws/` 假点，已标 `# INVALID` 保留）。⟹ **上界分布的最大值仍只压在一个点（88.3h）上**，这是「96 够不够」这个问题的证据基础薄弱之处，而它只能靠时间累积。
5. ⭐⭐ 上周（W34）那条产出分布仍未解决：**AWS 7/7 而其余三个日更任务只有 3–4 份**（实际完成率 AWS 100% / 其余 43–57%）。AWS 是唯一被 cron 可靠触发的任务，而我给不出机制。
