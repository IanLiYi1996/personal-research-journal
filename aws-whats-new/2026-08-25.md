# AWS What's New: 2026-08-25

- **抓取时间:** 2026-08-25 09:15:45 UTC
- **过去 24h 公告数:** 8 ＋ **补录 4 条**（`ᴮ`，pubDate 早于 24h 窗口但从未进过任何 digest）
- **Source:** https://aws.amazon.com/about-aws/whats-new/recent/feed/

## Top Highlights

- [SageMaker MLflow now supports customer managed keys](https://aws.amazon.com/about-aws/whats-new/2026/08/sagemaker-mlflow-custom-keys) — AI/ML
- [Amazon SageMaker HyperPod enhances support for Ray](https://aws.amazon.com/about-aws/whats-new/2026/08/amazon-sagemaker-hyperpod-ray) — AI/ML
- [Amazon Bedrock AgentCore Memory now supports extracting memories from non-conversational JSON payloads](https://aws.amazon.com/about-aws/whats-new/2026/08/agentcore-memory-json-payloads) — AI/ML

## 按类别详情

### AI/ML (6 项)

| 时间 (UTC) | 公告 | 影响 |
|------|------|------|
| 08-24 19:26 | [SageMaker MLflow now supports customer managed keys](https://aws.amazon.com/about-aws/whats-new/2026/08/sagemaker-mlflow-custom-keys) | High |
| 08-24 15:00 | [Amazon SageMaker HyperPod enhances support for Ray](https://aws.amazon.com/about-aws/whats-new/2026/08/amazon-sagemaker-hyperpod-ray) | High |
| 08-24 14:00 | [AWS ParallelCluster 3.16 adds an on-node diagnostics tool](https://aws.amazon.com/about-aws/whats-new/2026/08/aws-parallelcluster/) | Medium |
| 08-24 13:00 | [OpenAI GPT-5.6 Terra and Luna now available on Amazon Bedrock in AWS GovCloud (US)](https://aws.amazon.com/about-aws/whats-new/2026/08/openai-gpt-terra-luna-govcloud/) | Low |
| 08-21 21:11 ᴮ | [AWS Neuron 2.32 introduces expanded NKI programming, MXFP8 training kernels, and variable-size collectives for Trn2 and Trn](https://aws.amazon.com/about-aws/whats-new/2026/08/aws-announce-neuron-2-32-0) | Medium |
| 08-20 20:00 ᴮ | [Amazon Bedrock AgentCore Memory now supports extracting memories from non-conversational JSON payloads](https://aws.amazon.com/about-aws/whats-new/2026/08/agentcore-memory-json-payloads) | High |

### Compute (2 项)

| 时间 (UTC) | 公告 | 影响 |
|------|------|------|
| 08-24 22:42 | [Amazon ECS now automatically detects and repairs container instances with impaired agent connectivity](https://aws.amazon.com/about-aws/whats-new/2026/08/amazon-ecs-agent-connectivity-health) | Medium |
| 08-24 18:29 | [Amazon EKS now supports multiple external OIDC identity providers per cluster](https://aws.amazon.com/about-aws/whats-new/2026/08/amazon-eks-multiple-oidc-providers) | Medium |

### Database (3 项)

| 时间 (UTC) | 公告 | 影响 |
|------|------|------|
| 08-24 16:00 | [Amazon Aurora now supports PostgreSQL 18.4, 17.10, 16.14, 15.18, and 14.23](https://aws.amazon.com/about-aws/whats-new/2026/08/amazon-aurora-postgresql-18-4-17-10-16-14-15-18-14-23/) | Medium |
| 08-24 08:41 ᴮ | [Amazon RDS for MySQL now supports new minor version 8.4.11](https://aws.amazon.com/about-aws/whats-new/2026/08/amazon-rds-mysql-8411-available/) | Low |
| 08-20 20:00 ᴮ | [Amazon Redshift now supports concurrency scaling of streaming ingestion workloads from Amazon Kinesis data streams](https://aws.amazon.com/about-aws/whats-new/2026/08/redshift-concurrencyscaling-for-kds-streams/) | Medium |

### 其他 (1 项)

| 时间 (UTC) | 公告 | 影响 |
|------|------|------|
| 08-24 14:46 | [Amazon Connect Customer now supports information extraction for agent voice and chat conversations](https://aws.amazon.com/about-aws/whats-new/2026/08/amazon-connect-customer-information/) | Medium |

## 小结与趋势

### 🚨⭐⭐⭐ `BACKFILL_HOURS = 168` 第一次挣到钱：`(96,168]` 复核连续四次为空后，今天有 2 条

前四次复核（08-21 5 条 / 08-22 14 条 / 08-23 26 条 / 08-24 45 条）**全部 0 未覆盖**，而 08-24 我按「代价不对称」决定不收回到 96。**今天第五次复核：46 条里 2 条未覆盖，都在 109.1h**：

| pubDate | 上界 | 下界 | 条目 |
|---|---|---|---|
| 08-20 20:00 | **109.1h** | 85.1h | Redshift 流式摄取并发扩展 |
| 08-20 20:00 | **109.1h** | 85.1h | AgentCore Memory 支持非会话 JSON 载荷（本份 High 之一） |

⭐⭐⭐ **而它们在 96 下是「必然丢失」而非「可能丢失」，这一点可以严格论证**（日更 09:04，故对 pubDate 08-20 20:00 而言可用的运行龄只有 13.1 / 37.1 / 61.1 / 85.1 / **109.1** 这一串）：**85.1h 那次已证明它们不在 feed 里**（下界就是这么来的），而下一次运行是 109.1h ＝ **已在 96 窗口外** ⟹ 96 下每一次运行都看不到它们。⭐ **结论不依赖真实可见时刻落在 [85.1, 109.1] 的哪里。**

⭐⭐⭐ **由此纠正 CLAUDE.md 里记的边际算法：日更下的「保证覆盖上限」是 `W − 运行间隔` 而不是 `W`。** 因为最坏情况是条目刚好在某次运行后才可见，下一次机会已过去 24h。⟹ **W=96 只保证覆盖 ≤72h 的延迟，而今天这个事件的下界 85.1h 本身就超了 72h**；W=168 保证 ≤144h，相对今天的 109.1h 仍有约 35h 余量。⚠️ **所以 CLAUDE.md 记的「96 只剩 8–36 小时边际」在最坏情况下其实已经是负的**（88.3 + 24 = 112.3 > 96），当时那句话把 `W − delay` 当成了边际。⟹ **不收回 168 现在有实测依据，不再只是代价不对称的论证。**

⚠️ **已按 08-20 那条教训逐条回到原始数据核实**（当时四个「补录点」里三个是 `/about-aws/` URL 变体造成的假点）：四条今天新增的补录**在 feed 里各只出现 1 次、只有 1 种 URL 形式、且只在今天这份 digest 里出现过** ⟹ 全部为真。

### ⭐⭐⭐ 周一那批 9 条全部落进周二这份，且是 9/9 的干净对账

CLAUDE.md 预测过「周一 09:0x 报 0 条是正常形态，条目会落进周二那份 digest」。今天是这条预测第一次拿到**完整对账**：**dirs API 显示周一 08-24 共 9 条**，而本份的 8 条新鲜条目全部是 08-24 的，剩下 1 条（RDS MySQL 8.4.11，08:41）由补录机制捞回 ⟹ **8 + 1 = 9，没有任何丢失，错位只在文件名上。**

⚠️⚠️ **但它同时暴露「首条早于 09:04」这个统计量是有歧义的，而两种读法今天第一次在真实案例上分岔**：周一 08-24 的首条 pubDate 是 **08:41（早于 09:04）**，按 pubDate 读该把 Monday 从 4/9 更新成 **5/10**；⭐ **然而昨天 09:04 那次运行报 0 条、当时 feed 最新条目距抓取 60.9 小时** ⟹ **这条 08:41 的条目在运行时根本不可见**（它的延迟区间是 0.4–24.4h）。⟹ ⭐⭐⭐ **那个统计量当初是为回答「09:04 的 cron 能不能看到当天产出」而建的，而这个问题要的是「运行时可见」不是「pubDate 早」；由于 feed 会回填，pubDate 会系统性高估 cron 本可看到的量。故按它本来的口径 Monday 仍是 4/10。** ⭐ 这与 08-19 修正 Tuesday 那次是同一个回填假象，只是方向相反（那次回填让「0 条」看起来推翻了断言，这次回填让一个 pubDate 很早的条目其实不可见）。

⭐ 顺带：dirs 的返回里 **08-22（六）与 08-23（日）完全缺席 ＝ 周末严格为零再次成立**；周一 n=9 与 CLAUDE.md 记的「周一均值 10.2 条」量级一致 —— **周一是「启动很晚」而不是「发得少」，再次确认。**

### ⭐⭐ 分类漂移第 7 例已当场修掉：Amazon Connect 是全表最散的服务（14 条散在 6 个类别）

本次 `Amazon Connect Customer ... information extraction` 被分到 **Storage**。诊断（复现 `classify` 的双通道控制流后）：**Connect 在关键词表里根本没有条目**（只有 `direct connect` / `interconnect` / `mediaconnect`）⟹ 标题通道 0 命中 → 落到全文通道，全文里只有两个命中 `kinesis`(pos 960) 与 `s3 `(pos 986)，⭐ **而全文通道按类别顺序而非位置定胜负，故 Storage 赢了 Analytics —— 两个都是错的。**

⭐⭐⭐ **按 CLAUDE.md「记录一个已知缺陷 ≠ 修掉它」当场量了影响面，而量出来的结果比预期严重得多：历史上 14 条 Connect 公告散落在 6 个类别** —— 其他 7 / Management 3 / AI/ML 2 / Storage 1 / **医疗·生命科学 1**（一条「坐席队列分析仪表盘」进了医疗，明显错）⟹ **远不是 n=1，是本 tracker 里最散的服务。**

✅ **修法：加 `NO_CATEGORY` 钉住表，把「没有对应类别的服务」钉到兜底类 其他** —— 依据是 CLAUDE.md 08-15 已记下的判断「SES 是邮件、Connect 是联络中心仪表盘，**兜底类才是诚实位置**」，且 其他 本来就是 14 条里的众数（7 条）。⭐ **没有为一个服务新建类别**（08-20 已记「为 n=1 新建类别是过拟合」，而这里即便 n=14 也仍无「联络中心」这一类，钉到兜底比发明一类更诚实）。

⭐⭐ **钉住是「位置感知」的而不是短路的，这一步是我自己的合成测试逼出来的**：第一版一看到标题里有 `amazon connect` 就返回 其他，于是 **「Amazon **Bedrock** now integrates with Amazon Connect」被误钉走** —— 主体是 Bedrock。改成沿用 `classify` 自己的「标题里最早命中的关键词胜出」规则后，6 条守卫全过（Direct Connect / MediaConnect 不动、Bedrock 与 Config 保住自己的主体、两条真 Connect 进 其他）。⭐ **影响面：100 条 feed 上恰好翻转 1 条、即目标条目、0 处附带误伤。**

### ⚠️ 我这次自己踩的：探查脚本用相对路径，使 `p == out_path` 那道跳过静默失效

跑 `(96,168]` 复核时我按 CLAUDE.md 的指示复用了脚本自己的 `_already_covered()`，并判断「它显式跳过 `out_path`，所以写盘前后调用都安全」。**这个判断本身对，但我传进去的是 `Path('aws-whats-new')/'2026-08-25.md'`，而 `OUT_DIR` 是绝对路径 ⟹ 两者永不相等 ⟹ 今天这份 digest 没有被排除，于是刚写进去的条目全部算成「已覆盖」，复核报 0。**

⭐⭐⭐ **抓到它的唯一原因是我把整个年龄分布和「全区间未覆盖数」也打了出来：`(0,168] 未覆盖 = 0` 与「我刚写了 12 条」直接矛盾，刺眼。** 修正后 `(0,168] 未覆盖 = 12` 恰好等于 8 新鲜 + 4 补录 ＝ 自洽。⟹ ⭐⭐ **这是「探查脚本比被测逻辑粗糙」的第 3–4 次，而这次的粗糙之处是「相对 vs 绝对路径」，不是正则宽窄也不是参数顺序** ⟹ **判据要再扩一格：复用被测脚本的辅助函数时，连传进去的路径对象都要用它自己的构造方式（`A.OUT_DIR / ...`），不要手搓等价物。** ⭐ 同一轮里我还两次让探查脚本比 `classify` 粗糙（漏了 lowercase、漏了双通道），也都是靠「结果与另一个已知量矛盾」发现的。

### 本份内容侧

- **AI/ML 6 条为最大类**，含两条 SageMaker High（MLflow 支持客户托管密钥、HyperPod 强化 Ray 支持）与补录的 **AgentCore Memory 支持从非会话 JSON 载荷抽取记忆** —— ⭐ 后者接上我追的「记忆不能只当检索」那条线：**记忆来源从对话扩到结构化载荷，意味着 agent 记忆的输入面在变宽**，而 CLAUDE.md 里记的 AgentCore Memory 相关公告（policy harness / GovCloud）说明这条产品线本月一直在动。
- **GPT-5.6 Terra 与 Luna 进 GovCloud** —— 接 W34 cross-digest Arc 3 记的「降价沿能力阶梯上行」那条线的另一半：**分发面（GovCloud）与价格面在同一个月分别推进**。⚠️ 本条只是区域上线，标 Low。
- **Neuron 2.32**（补录）带 MXFP8 训练 kernel 与可变长 collectives，Trn2/Trn 侧；**ECS 自动检测并修复 agent 连接受损的容器实例** ⭐ 属「让静默失败变成可检测/自愈」那一族，与我在本 tracker 自己踩的守卫问题同一取向。
- **EKS 支持每集群多个外部 OIDC 身份提供方**、**Aurora PostgreSQL 五个小版本**、**RDS MySQL 8.4.11**（补录）为常规增量。

## Open Questions

1. ⭐⭐⭐ **`(96,168]` 那条复核仍然依赖我每天手工跑。** CLAUDE.md 08-24 已把它列为待办（「应把它做成脚本里的一行自动检查，未覆盖数 > 0 时打 stderr 告警」），而**今天正是它第一次返回非零的日子——如果我今天没跑，这两条会静默进 digest 而我不会知道加宽挣到了钱**。⟹ 按「会静默的失败必须变成硬失败」，这次的对象是**我自己的检验动作**，且今天证明了它确实会有非零结果。**优先级应从「待办」升为「下次动脚本时一并做掉」。**
2. ⚠️ 今天两条 109.1h 的 pubDate 完全相同（08-20 20:00），而 08-22 还有第三条同 pubDate 的（EC2 c8gd/m8gd，当时 37.1h）⟹ **同一个整点档位下的三条，发现时刻却差 72 小时**。若是同一次 feed 批处理故障，三条该同时出现；故我倾向按**三个独立事件**记账，⚠️ 但这与 CLAUDE.md「必须按事件聚合」那条的判据边界还没划清——**「同 pubDate」不足以说明「同一个事件」，因为整点 pubDate 本身是排期档位。**
3. ⚠️ Connect 钉到 其他 是按「主体归类」做的，但**本条公告的实际能力（对话信息抽取、推断洞察）确实是 AI/ML 性质** ⟹ 若日后 其他 里堆积的 Connect 条目多到掩盖内容，正确动作可能是**新建一个「业务应用 / 联络中心」类别**而不是继续钉兜底。⭐ 判据：等 其他 里的 Connect 条目影响到我读 digest 时再动，现在 n=14 但每天只 0–2 条。
4. 🚨⭐⭐⭐ **心跳 `check` 报 ANOMALY，且不对称形态是历次最宽的**：`CronList` 六个任务全在、id 与 08-24 重建时逐个相同，然而 **hf 连续 4 天（08-21~08-24）、reddit 与 tech-blogs 各连续 3 天（08-22~08-24）完全无行**，只有 aws 在跑。⟹ 「重建 cron 修不了跑不起来」再次确认（这次是在重建后的第 1 天就已恢复成旧形态）。
   - ⭐ **顺带更正我本份初稿写错的一处**：cron 到期不是今天 —— 08-24 那次重建把时钟重置了，按「7 天后过期」应是**约 08-31**；我把 W34 cross-digest 里针对**上一次（08-18）重建**写的「约 08-25 到期」误当成了当前状态。
   - ⚠️ **本次触发方式我判定不了**：心跳行的时刻是 **09:04**，与 aws 的 cron 时刻**精确对齐**，而这正是 CLAUDE.md 用来判定「确实被 cron 触发过」的那条判据；但我这一侧收到的是带参数的 slash command，与手动触发不可区分。⟹ **只能说「时刻对齐、与 cron 触发一致」，不能断言它是 cron 触发，也因此不单独把今天计入那条连续天数。**
