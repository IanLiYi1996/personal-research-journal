# AWS What's New: 2026-08-23

- **抓取时间:** 2026-08-23 09:04:14 UTC
- **过去 24h 公告数:** 0
- **Source:** https://aws.amazon.com/about-aws/whats-new/recent/feed/


过去 24h RSS 无新条目。

## 小结与趋势

### 「0 条」已按规矩拆成两个可分别核验的来源，且这次两边都很干净

CLAUDE.md 要求报 0 时必须区分 **(a) 上游确实没发** 与 **(b) 窗口滑动造成进出相抵**，因为事后无法从产出里区分。本次：

- **(a) 上游没发。** 独立解析 feed：**最新条目是 08-21 20:11 UTC（Bedrock 给 GPT-5.6 Sol 降价），距抓取时刻 36.9h**；本次 24h 窗口（08-22 09:05 → 08-23 09:05）内 **0 条**。⭐ 而 feed 的按日分布直接给出理由：**08-22 与 08-23 各 0 条**。
- **(b) 不是进出相抵，是「5 出 0 进」。** 两次运行的 24h 下界之间（08-21 09:15 → 08-22 09:05）有 **5 条**滑出（即脚本自报的 `kept N carried` 的 N=5），⭐ **而这 5 条逐条对回 08-22 那份，5/5 全部已被列出** ⟹ 滑动没有掩盖任何变化。
- 兜底：**整个 `BACKFILL_HOURS=168` 范围内 63 条，0 条未覆盖**（用脚本自己的 `_already_covered()` + `_canon_link()` 判定）⟹ 没有条目被丢。

⟹ **今天的 0 是无歧义的 0。** ⭐ 这也是我第一次遇到 (b) 侧不是「0 条滑出」而是「5 条滑出但全部已覆盖」的形态 —— 后者比前者更有信息量，因为它顺带证明了**上一次运行确实完整接住了那批周五傍晚的条目**。

### 周末形态：今天是周日，且它连「接收窗口」的活也没干

今天是**周日**。CLAUDE.md 记过一条比「周末照跑」原始理由更强的推论：**周末的运行价值在于接收工作日迟到条目**，而不是抓周末产出（08-15 周六收 2 条补录、08-22 周六收 3 条补录都印证了这条）。

⭐⭐ **但本次周日一条补录都没有，而这与已测出的延迟分布自洽**：有效延迟上界集中在 **25–88h**，故周五傍晚那批的迟到条目**会在周六那次被接住**，到周日已全部覆盖 ⟹ **修正一句措辞：周末两天的角色不对称 —— 周六是接收窗口，周日基本是空跑。** ⚠️ 这是从一天观测 + 已有延迟分布推的，n=1；下一个检验点是下个周日看是否同样为 0 条补录。

⭐ 顺带一个「周末严格为零」的新证据形式：**本次 100 条 feed 窗口跨 08-10 → 08-21，而 08-15 与 08-16 在按日分布里完全缺席** —— 此前这条断言都是靠 dirs API 逐日统计得来的，这次是**在单份 feed 内部**直接可见。

### `BACKFILL_HOURS` 96→168 的第三次独立复核：仍然是空的

换一天重跑同一检验：**feed 里落在 (96, 168]h 的条目共 26 条，26 条全部已被此前 digest 覆盖，0 条未覆盖**（08-21 是 5/5、08-22 是 14/14）。

⟹ ⭐⭐ **连续三天、样本量递增（5 → 14 → 26）都什么也没捞回来。** 按 08-21 立下的收回判据，168 现在更接近**一次已完成的测量**（结论：那个区间是空的）而非需长期保留的余量 —— 倾向解释仍是「96h 附近那两个点是两个事件的巧合」而不是「分布被截断」。

⚠️ 但**不动它**，理由不变：最大上界 **88.3h**、第二高 **86.5h**，两点距 96 都在 10h 内，收回即刻回到比值 1.09；而加宽的代价只是每次多扫的对账量。**判据仍是看上界分布的最大值，要证据不要感觉。**
⚠️ 另注意本次这 26 条对应 08-16 → 08-19，其中 08-16 是周末（0 条）⟹ 有效覆盖只有三个工作日，样本量比「26 条」听起来的要窄一点。

### ⭐⭐⭐ 一个流程实例：那个「运行集合必须排除已报出该条目的运行」的 bug 第三次出现，而这次它出现在我的探查脚本里

算 (b) 时我第一次拿到的答案是 **0 条滑出**，而正确答案是 5 —— 因为我调了脚本自己的 `_prior_run_times()`，它把**本次运行自己的 header 也算了进来**（脚本已先写盘），于是「上次运行」= 本次运行，两个 24h 下界相同、区间宽度为 0。

- ⭐⭐ **这正是 CLAUDE.md 里记过两次的同一个 bug 形态**（08-17「`_prior_run_times()` 把当前这次运行也算进去」、08-18「同日更早那次运行恰恰是报出该条目的那一次」），**而这次它不在脚本里、在我临时写的探查脚本里。**
- ⭐⭐⭐ **由此得到一条对「复用被测脚本自己的辅助函数」这条判据的实质修正**：复用它继承的不只是逻辑，**还继承了它对「何时被调用」的假设**。`_prior_run_times()` 是为 `main()` **写盘之前**的调用点写的；在写盘**之后**调它，同一份代码给出不同答案。⟹ **判据应改成「复用逻辑，但要独立确认调用时机的前提仍然成立」** —— 而不是我原来那条更简单的「直接复用就对了」。
- ⭐ **抓到它的唯一原因是我把两个时刻都打印了出来**，于是「上次运行 08-23 09:04 → 本次 08-23 09:04」这行自相矛盾得很刺眼。这是 08-20 那条「探查脚本必须留一个结果显然错就会刺眼的对照量」第二次真正承重。⚠️ **而值得警惕的是：若我只打印那个 0，它看起来完全合理**（周日、上游没发、0 条滑出），我会把它当成 (b) 的答案写进 digest —— **一个错误的核验通过，比没做核验更糟。**

### 🚨⭐⭐⭐ 心跳报 ANOMALY：六个 cron 里只有 AWS 这一个在跑，而「较晚的班次更容易跑起来」这个假说被否掉了

`check` 报 ANOMALY，三个独立来源逐条一致（心跳行 / 文件 mtime / git log）：

| 任务 | 心跳最后一行 | 产出 mtime | 应跑而没跑 |
|---|---|---|---|
| aws (09:04) | **08-23 09:07 done** | 08-23 | — |
| hf (07:57 + 17:41) | 08-20 05:38 | 08-20 05:37 | 08-21 / 08-22 / **08-23** |
| reddit (08:13) | 08-21 05:24 | 08-21 05:23 | 08-22 / **08-23** |
| tech-blogs (08:22) | 08-21 05:18 | 08-21 05:18 | 08-22 / **08-23** |
| cross-digest (周五 09:41) | **从未写过行** | `weekly/` 最新仍是 08-14 的 W33 | W34（08-21） |

⭐⭐ **`CronList` 显示 6 个任务全在，id 与 08-18 重建时逐个相同**（`583e91d9`/`aff14d97`/`f82fb2ad`/`fee9d93a`/`0db21242`/`9704fdaa`），**且每个 prompt 第一行就是 `runlog.py start`** ⟹ 连第一条命令都没执行 ⟹ **失败发生在「会话开始执行之前」，与 08-20 那次同一形态**（这是「任务在但会话没启动」的第 5 次，08-22 与 08-23 是同一段的连续两天）。

⭐⭐⭐ **但本次拿到一个此前没有的、更强也更具体的事实，而它推翻了 CLAUDE.md 里那个暂定假说。** 把心跳覆盖期（08-18 起）内每一行的**时刻**与各任务的 cron 时刻逐一对照：

- **aws (09:04)：08-19 / 08-20 / 08-21 / 08-22 / 08-23 连续 5 天都恰在 09:04 起跑** ＝ 唯一一个确实从 cron 触发过的任务。
- **其余五个：0 次。** hf 的 08-20 05:24、reddit 与 tech-blogs 的 08-21 04:53 **都不是它们的 cron 时刻**（07:57/17:41、08:13、08:22）⟹ 那些是手敲 slash command 的补跑，不是 cron。

⟹ ⭐⭐⭐ **CLAUDE.md 里我写的「较晚的班次似乎更容易跑起来」不成立：hf 的 17:41 比 aws 的 09:04 更晚，而它在整个覆盖期内一次都没产出过行。** 故 aws 能跑起来的原因不是「排得晚」，我仍给不出机制。
⚠️ **必须标清这条断言的边界**：「触发了但会话在第一条命令前就死」与「根本没触发」在我这端**产出完全一样**（都是无行）⟹ 严格说我能断言的是「**只有 09:04 那个任务产生过心跳行**」，不能断言另外五个「没被触发」。⭐ 而这恰好是心跳设计本身的边界——它区分了「没启动 / 跑了没完成」，但区分不了「没被触发 / 触发了但没启动」。

⚠️⚠️ **两件需要人决定的事（我没有动，因为动错方向的代价不对称）**：
1. **cron 按「最后触发一次后 7 天」过期，08-18 重建 ⟹ 约 08-25 到期，还有约 2 天。** ⚠️ 但**重建修不了当前这个问题**——任务还在、没过期、prompt 正确，所以「重建」和「跑不起来」是两件独立的事；⭐ 且 CLAUDE.md 明确警告**旧任务仍存活时直接新建会变成双份触发**。
2. **代价按「可否回溯」排（08-15 那条规则）**：HF 能完整补回（日期桶按日期索引）；**reddit 的 top-of-week 只有 7 天跨度**，08-22 起算还兜得住、再拖就永久丢；**tech-blogs 最浅的几个 feed 深度不足一天（arXiv cs.AI / LessWrong / 量子位），08-21~08-23 那几天的内容大概已经没了**。

## Open Questions

- **下个周日是否同样 0 条补录？** 若是，「周六接收 / 周日空跑」这条不对称就从 n=1 变成可用的形态判断；若不是，说明延迟分布的尾巴比 88.3h 长。
- **延迟日志停在 15 条有效行 / 10 个粗事件已经三天没动**（因为连续三天没有补录条目）。⚠️ 这意味着「看上界分布最大值」这个判据在低补录期**不会积累新证据**，故 168 这个值可能长期停在「测量已完成但不敢收回」的状态 —— 需要想清楚这算不算一个该接受的稳态。
